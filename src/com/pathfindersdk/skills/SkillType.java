package com.pathfindersdk.skills;

/**
 * The Enum Skill.
 */
public enum SkillType
{
  
  /** Acrobatics skill. */
  ACROBATICS ("Acrobatics"),
  
  /** Appraise skill. */
  APPRAISE ("Appraise"),
  
  /** Bluff skill. */
  BLUFF ("Bluff"),
  
  /** Climb skill. */
  CLIMB ("Climb"),
  
  /** Craft (alchemy) skill. */
  CRAFT_ALCHEMY ("Craft (alchemy)"),
  
  /** Craft (armor) skill. */
  CRAFT_ARMOR ("Craft (armor)"),
  
  /** Craft (baskets) skill. */
  CRAFT_BASKETS ("Craft (baskets)"),
  
  /** Craft (books) skill. */
  CRAFT_BOOKS ("Craft (books)"),
  
  /** Craft (bows) skill. */
  CRAFT_BOWS ("Craft (bows)"),
  
  /** Craft (calligraphy) skill. */
  CRAFT_CALLIGRAPHY ("Craft (calligraphy)"),
  
  /** Craft (carpentry) skill. */
  CRAFT_CARPENTRY ("Craft (carpentry)"),
  
  /** Craft (cloth) skill. */
  CRAFT_CLOTH ("Craft (cloth)"),
  
  /** Craft (clothing) skill. */
  CRAFT_CLOTHING ("Craft (clothing)"),
  
  /** Craft (glass) skill. */
  CRAFT_GLASS ("Craft (glass)"),
  
  /** Craft (jewelry) skill. */
  CRAFT_JEWELRY ("Craft (jewelry)"),
  
  /** Craft (leather) skill. */
  CRAFT_LEATHER ("Craft (leather)"),
  
  /** Craft (locks) skill. */
  CRAFT_LOCKS ("Craft (locks)"),
  
  /** Craft (paintings) skill. */
  CRAFT_PAINTINGS ("Craft (paintings)"),
  
  /** Craft (pottery) skill. */
  CRAFT_POTTERY ("Craft (pottery)"),
  
  /** Craft (sculptures) skill. */
  CRAFT_SCULPTURES ("Craft (sculptures)"),
  
  /** Craft (ships) skill. */
  CRAFT_SHIPS ("Craft (ships)"),
  
  /** Craft (shoes) skill. */
  CRAFT_SHOES ("Craft (shoes)"),
  
  /** Craft (stonemasonry) skill. */
  CRAFT_STONEMASONRY ("Craft (stonemasonry)"),
  
  /** Craft (traps) skill. */
  CRAFT_TRAPS ("Craft (traps)"),
  
  /** Craft (weapons) skill. */
  CRAFT_WEAPONS ("Craft (weapons)"),
  
  /** Diplomacy skill. */
  DIPLOMACY ("Diplomacy"),
  
  /** Disable Device skill. */
  DISABLE_DEVICE ("Disable Device"),
  
  /** Disguise skill. */
  DISGUISE ("Disguise"),
  
  /** Escape Artist skill. */
  ESCAPE_ARTIST ("Escape Artist"),
  
  /** Fly skill. */
  FLY ("Fly"),
  
  /** Handle Animal skill. */
  HANDLE_ANIMAL ("Handle Animal"),
  
  /** Heal skill. */
  HEAL ("Heal"),
  
  /** Intimidate skill. */
  INTIMIDATE ("Intimidate"),
  
  /** Knowledge (arcana) skill. */
  KNOWLEDGE_ARCANA ("Knowledge (arcana)"),
  
  /** Knowledge (dungeonneering) skill. */
  KNOWLEDGE_DUNGEONNEERING ("Knowledge (dungeoneering)"),
  
  /** Knowledge (engineering) skill. */
  KNOWLEDGE_ENGINEERING ("Knowledge (engineering)"),
  
  /** Knowledge (geography) skill. */
  KNOWLEDGE_GEOGRAPHY ("Knowledge (geography)"),
  
  /** Knowledge (history) skill. */
  KNOWLEDGE_HISTORY ("Knowledge (history)"),
  
  /** Knowledge (local) skill. */
  KNOWLEDGE_LOCAL ("Knowledge (local)"),
  
  /** Knowledge (nature) skill. */
  KNOWLEDGE_NATURE ("Knowledge (nature)"),
  
  /** Knowledge (nobility) skill. */
  KNOWLEDGE_NOBILITY ("Knowledge (nobility)"),
  
  /** Knowledge (planes) skill. */
  KNOWLEDGE_PLANES ("Knowledge (planes)"),
  
  /** Knowledge (religion) skill. */
  KNOWLEDGE_RELIGION ("Knowledge (religion)"),
  
  /** Linguistics skill. */
  LINGUISTICS ("Linguistics"),
  
  /** Perception skill. */
  PERCEPTION ("Perception"),
  
  /** Perform (act) skill. */
  PERFORM_ACT ("Perform (act)"),
  
  /** Perform (comedy) skill. */
  PERFORM_COMEDY ("Perform (comedy)"),
  
  /** Perform (dance) skill. */
  PERFORM_DANCE ("Perform (dance)"),
  
  /** Perform (keyboard) skill. */
  PERFORM_KEYBOARD ("Perform (keyboard)"),
  
  /** Perform (oratory) skill. */
  PERFORM_ORATORY ("Perform (oratory)"),
  
  /** Perform (percussion instruments) skill. */
  PERFORM_PERCUSSION ("Perform (percussion instruments)"),
  
  /** Perform (string instruments) skill. */
  PERFORM_STRING ("Perform (string instruments)"),
  
  /** Perform (wind instruments) skill. */
  PERFORM_WIND ("Perform (wind instruments)"),
  
  /** Perform (sing) skill. */
  PERFORM_SING ("Perform (sing)"),
  
  /** Profession (architect) skill. */
  PROFESSION_ARCHITECT ("Profession (architect)"),
  
  /** Profession (baker) skill. */
  PROFESSION_BAKER ("Profession (baker)"),
  
  /** Profession (barrister) skill. */
  PROFESSION_BARRISTER ("Profession (barrister)"),
  
  /** Profession (brewer) skill. */
  PROFESSION_BREWER ("Profession (brewer)"),
  
  /** Profession (butcher) skill. */
  PROFESSION_BUTCHER ("Profession (butcher)"),
  
  /** Profession (clerk) skill. */
  PROFESSION_CLERK ("Profession (clerk)"),
  
  /** Profession (cook) skill. */
  PROFESSION_COOK ("Profession (cook)"),
  
  /** Profession (courtesan) skill. */
  PROFESSION_COURTESAN ("Profession (courtesan)"),
  
  /** Profession (driver) skill. */
  PROFESSION_DRIVER ("Profession (driver)"),
  
  /** Profession (engineer) skill. */
  PROFESSION_ENGINEER ("Profession (engineer)"),
  
  /** Profession (farmer) skill. */
  PROFESSION_FARMER ("Profession (farmer)"),
  
  /** Profession (fisherman) skill. */
  PROFESSION_FISHERMAN ("Profession (fisherman)"),
  
  /** Profession (gambler) skill. */
  PROFESSION_GAMBLER ("Profession (gambler)"),
  
  /** Profession (gardener) skill. */
  PROFESSION_GARDENER ("Profession (gardener)"),
  
  /** Profession (herbalist) skill. */
  PROFESSION_HERBALIST ("Profession (herbalist)"),
  
  /** Profession (innkeeper) skill. */
  PROFESSION_INNKEEPER ("Profession (innkeeper)"),
  
  /** Profession (librarian) skill. */
  PROFESSION_LIBRARIAN ("Profession (librarian)"),
  
  /** Profession (merchant) skill. */
  PROFESSION_MERCHANT ("Profession (merchant)"),
  
  /** Profession (midwife) skill. */
  PROFESSION_MIDWIFE ("Profession (midwife)"),
  
  /** Profession (miller) skill. */
  PROFESSION_MILLER ("Profession (miller)"),
  
  /** Profession (miner) skill. */
  PROFESSION_MINER ("Profession (miner)"),
  
  /** Profession (porter) skill. */
  PROFESSION_PORTER ("Profession (porter)"),
  
  /** Profession (sailor) skill. */
  PROFESSION_SAILOR ("Profession (sailor)"),
  
  /** Profession (scribe) skill. */
  PROFESSION_SCRIBE ("Profession (scribe)"),
  
  /** Profession (shepherd) skill. */
  PROFESSION_SHEPHERD ("Profession (shepherd)"),
  
  /** Profession (stable master) skill. */
  PROFESSION_STABLE_MASTER ("Profession (stable master)"),
  
  /** Profession (soldier) skill. */
  PROFESSION_SOLDIER ("Profession (soldier)"),
  
  /** Profession (tanner) skill. */
  PROFESSION_TANNER ("Profession (tanner)"),
  
  /** Profession (trapper) skill. */
  PROFESSION_TRAPPER ("Profession (trapper)"),
  
  /** Profession (woodcutter) skill. */
  PROFESSION_WOODCUTTER ("Profession (woodcutter)"),
  
  /** Ride skill. */
  RIDE ("Ride"),
  
  /** Sense Motive skill. */
  SENSE_MOTIVE ("Sense Motive"),
  
  /** Sleight of Hand skill. */
  SLEIGHT_OF_HAND ("Sleight of Hand"),
  
  /** Spellcraft skill. */
  SPELLCRAFT ("Spellcraft"),
  
  /** Stealth skill. */
  STEALTH ("Stealth"),
  
  /** Survival skill. */
  SURVIVAL ("Survival"),
  
  /** Swim skill. */
  SWIM ("Swim"),
  
  /** Use Magic Device skill. */
  USE_MAGIC_DEVICE ("Use Magic Device");

  /** Skill name. */
  private final String name;
  
  /**
   * Instantiates a new skill.
   *
   * @param name skill name
   */
  private SkillType(String name)
  {
    this.name = name;
  }
  
  public String getName()
  {
    return name;
  }
  
}
